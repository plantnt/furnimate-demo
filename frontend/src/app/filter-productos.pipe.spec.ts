import { FilterProductosPipe } from './filter-productos.pipe';

describe('FilterProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
